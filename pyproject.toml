[project]
name = "switchboard"
version = "0.1.0"
description = "An API gateway for raw Darwin and NR allocation feeds"
readme = "README.md"
authors = [
    { name = "Lorenzo", email = "lorenzo@lrzodev.xyz" }
]
requires-python = ">=3.11"
dependencies = []